# Soulbyte — sitio y onboarding de WhatsApp (Tech Provider de Meta)

Sitio estático en **https://soulbyte.app** (desde el 23-sep-2026), servido por Railway: servicio `soulbyte-site` (`88c37be1-103c-4408-acda-1297acd27b55`) del proyecto `soulbyte` (`5ba8a3d2-b987-460a-8bc1-b56c46d54a1c`, entorno production `58731972-95f6-4fed-81e7-483c0522ac46`), construido con Railpack desde `main` (raíz). El sitio pasó a ese proyecto el 23-sep-2026 con sus dominios; el servicio del mismo nombre en el proyecto EKIVIBES OK (`e4b71f90-ef42-4df7-b7ef-4407653e9295`) quedó sin dominios: no se despliega ahí. El push no despliega solo: cada publicación se despliega con la API de Railway (`serviceInstanceDeployV2` con el SHA del commit) hasta conectar el servicio a la rama; el token de proyecto de EKIVIBES OK no alcanza el proyecto `soulbyte`. `www.soulbyte.app` sirve lo mismo. DNS en Cloudflare: CNAME de la raíz y de `www` a Railway, sin proxy, con sus TXT `_railway-verify`. GitHub Pages sigue publicando la misma rama en https://satoshigod.github.io/soulbyte/ (dirección anterior); las URLs canónicas apuntan a soulbyte.app.

| Ruta | Qué es |
|---|---|
| `index.html` | Portada de la plataforma. Rediseño del 23-sep-2026 al estilo de las plataformas de comercio líderes (Shopify, BigCommerce, Tiendanube): barra fija con dos menús desplegables separados (23-sep-2026): «E-commerce» (Plataforma, WhatsApp Business, Meta, Instagram, Google, Mayoristas B2B y Dropshipping, en dos columnas) y «Servicios» (Empresas de servicios, Desarrollo y agencias, Salud, Abogados y firmas), portada oscura con la ilustración del panel, franja de marcas e integraciones en producción, cuadrícula «Plataforma», un bloque por canal con su ilustración, datos y operación, comparación, cifras, cómo funciona, preguntas y pie en columnas (E-commerce, Canales, Servicios, Empresa y Contacto). Toda ilustración con cifras lleva la leyenda «datos de ejemplo»; las cifras reales (2 marcas, 0 %, fechas de acreditación) salen de hechos verificados. Contacto público: `hola@soulbyte.app` (reenvío con Cloudflare Email Routing). Sección `#motor-propio` (21-sep-2026): los cuatro diferenciadores frente a una tienda alquilada — motor multimarca con canal mayorista, reglas propias en carrito y pago, sin comisión de plataforma por venta, sitio y datos del comercio — más tabla comparativa con datos públicos de Shopify verificados ese día (comisión por pasarela externa 2 %/1 %/0,6 %/0,2 % según plan; Shopify Payments no opera en Colombia; B2B nativo en todos los planes desde abril de 2026 con tope de 3 catálogos; Plus desde USD 2.300/mes) y FAQ «¿Por qué no simplemente Shopify?». Si Shopify cambia precios o alcance, actualizar tabla, nota y FAQ. Sección `#whatsapp` (21-sep-2026): el bloque de WhatsApp instalado en producción ese día — menú de autoservicio (estado de pedido, guía de talla, catálogo, garantía, asesor), catálogo de Meta conectado al chat (feeds n8n cada hora, «Agregar al carrito»), WhatsApp Flows (`guia_talla`, `encuesta_postventa`), transaccional y postventa (+2 d, +10 d, +12 m), OTP (`codigo_verificacion`) y reglas de convivencia con el equipo humano; tarjeta ancha «Del carrito al pago» (23-sep-2026): el carrito que el cliente envía desde el catálogo (mensaje `order`) se convierte en un carrito de la tienda y recibe el botón «Pagar mi pedido» (`/co/carrito/<id>` en la tienda), con precios e inventario del motor, un enlace por tienda y un solo recordatorio en la misma conversación (workflows n8n `C6e0izizT192Bjjz` y `AOvkT7bliWuk8wkO`), más la pregunta frecuente «¿El cliente puede pagar dentro de WhatsApp?»; no menciona Instagram ni Marketing Messages (Messenger se describe en `#meta`). Sección `#meta` (22-sep-2026): las automatizaciones de n8n con Meta instaladas ese día — píxel + API de conversiones (Purchase con `event_id = order.id`, datos del cliente en SHA-256), públicos personalizados diarios (listas de clientes, públicos del sitio y similar 1 % Colombia), vigilancia de anuncios cada hora (rechazos, pausa y reactivación por stock, gasto diario, CPA), reporte diario con ROAS real y campañas borrador creadas en pausa desde NocoDB, página de Facebook (publicaciones programadas, novedades del catálogo, comentarios por reglas con respuesta privada) y Messenger + leads de clic a WhatsApp. Exclusiones (22-sep-2026): quien pide no estar (tabla NocoDB `meta_exclusiones`) o respondió STOP en WhatsApp nunca se sube y se retira de los públicos; las tiendas lo explican en su política de privacidad (sección 7) con aviso en el pie de cada página. La nota de la sección dice que hoy opera sobre los activos de nuestras marcas y que, para otros comercios, hace falta que Meta apruebe el acceso avanzado de la app a los permisos de páginas y anuncios, y aceptar las condiciones de públicos personalizados. Instagram tiene su propia sección, `#instagram` (22-sep-2026). «Qué incluye» (23-sep-2026): ocho tarjetas (Tienda, WhatsApp, Meta, Instagram, Google, Mayoristas, Datos y automatización y, a todo el ancho, Dropshipping con la clase `completa`); la sección `#datos` detalla las automatizaciones diarias, semanales y mensuales de n8n. Sección `#dropshipping` (23-sep-2026): servicios para dropshippers y vendedores de redes que abren una tienda por producto y la cambian según la tendencia — multitienda sobre el mismo motor, inventario centralizado (bodegas, compras a proveedores, movimientos y reposición en NocoDB), marketplace, WhatsApp oficial (confirmación, seguimiento y carrito abandonado con permiso), cobros con la pasarela de pagos del comercio, guías con Envia y catálogo en Google; la nota aclara que el marketplace se arma a pedido de cada cliente (sobre el motor o integrando una plataforma externa) y que TikTok está en camino. Enlazada en el menú, en «Qué incluye», en «Para quién», en el pie y en el JSON-LD (`hasOfferCatalog`, `audienceType`, `knowsAbout`). Franja `#servicios` (23-sep-2026), antes de `#motor-propio`: «¿Vendes servicios? También automatizamos tu empresa.», con tres baldosas (desarrollo y agencias, salud, abogados y firmas) que llevan a `automatizacion-empresas-de-servicios/`; la misma página está en «Para quién» y en el JSON-LD (`hasOfferCatalog`). Acreditaciones: Tech Provider de WhatsApp Business (App Review aprobado el 12-sep-2026, app "Soulbyte n8n" 1781576016329423, empresa Soulbyte S.A.S. 576096632544762 verificada) y cuenta avanzada de Google Merchant Center (aprobada el 3-sep-2026, cuenta principal 5847345525; subcuentas Hit-Air Colombia 5848128866 y Ekivibes 5847731944). Redes (23-sep-2026): el pie de todas las páginas enlaza la página de Facebook (https://www.facebook.com/soulbyteapp) y el Instagram (https://www.instagram.com/soulbyteapp/) de Soulbyte, con glifos de Simple Icons (CC0), y el JSON-LD Organization los declara en `sameAs`. |
| `conectar/` | Conexión de WhatsApp Business para comercios (23-sep-2026: formulario a la derecha y, en móvil, justo después del título), con dos vías: **registro guiado por Meta** (hosted Embedded Signup, `hosted_url`) y **registro integrado** (Embedded Signup v4 con el SDK JS, `config_id`). La página obtiene la configuración de n8n; nunca ve el App Secret ni tokens. |
| `conectar/tiktok/` | Conexión de la cuenta de TikTok de un comercio (23-sep-2026; `noindex` y fuera de `sitemap.xml` hasta que se habilite). Es la URL de redirección del titular de la cuenta en la app de Soulbyte en TikTok API for Business: TikTok vuelve con `code` (un solo uso, 10 minutos) y `state`. La página compara el `state` con el que guardó en `localStorage` (`sb_tiktok_state`), quita el código de la barra de direcciones y lo envía a n8n: `POST /webhook/soulbyte-tiktok-conexion` `{code, state}` → `{ok, cuenta: {usuario}, mensaje}`. El botón «Conectar con TikTok» solo se habilita si `GET /webhook/soulbyte-tiktok-config` devuelve `{habilitado: true, auth_url}`; n8n lo arma con NocoDB `parametros` (`tiktok_app_id`, `tiktok_auth_url` y `tiktok_conexion_activa = si`). Mientras esos webhooks no existan, el botón sigue deshabilitado con la nota de espera. |
| `whatsapp-business-api/` | Página de canal (fase 2 del plan SEO, 23-sep-2026) para la búsqueda «API de WhatsApp Business» en Colombia: qué hace en el número del comercio (autoservicio, catálogo, carrito que se paga en la tienda, Flows, avisos de pedido, código de verificación y convivencia con el equipo), el carrito paso a paso, costos (Meta cobra por mensaje de plantilla; las respuestas dentro de las 24 horas y las plantillas de utilidad en ese lapso no se cobran; 72 horas gratis desde un anuncio de clic a WhatsApp o el botón de la página de Facebook; enlace a los precios oficiales de Meta), app frente a API, conexión en tres pasos y 7 preguntas. Marcado BreadcrumbList y Service, sin FAQPage. No promete coexistencia (app y API en el mismo número) mientras no esté activa para comercios. Enlazada desde el menú «E-commerce», el pie, la sección `#whatsapp` y la pregunta de costos de la portada, y el anuncio de `conectar/`. Si Meta cambia precios o reglas, actualizar `#costos` y la pregunta «¿Cuánto cuesta?». |
| `ecommerce-b2b-mayoristas/` | Página de canal (fase 2 del plan SEO, 23-sep-2026) para «e-commerce B2B», «tienda para distribuidores» y «listas de precios mayoristas» en Colombia. Describe solo lo que corre para Hit-Air Colombia y Ekivibes: precio mayorista solo con la sesión iniciada (grupos de clientes y listas de precios de Medusa); alta de distribuidores desde NocoDB (n8n `gCAEjaqiIqTNSxIc` cada 5 min y `Y7DsUJKcFzgPtuXf` cada hora para quien aún no tenía cuenta; correo de activación y serie de bienvenida a los 3 y 10 días); cotizaciones con descuento por línea o global y vigencia que pasan a pedido borrador (`nS2gW47ZI4dWPxPu`, cada 10 min), con seguimiento a los 3, 7 y 14 días (`arjgHumJB73rcdLF`, 9:10); cartera con plazos (anticipo, contado, 15, 30 o 60 días) y cupo (`aKzELbtHWttKrkK5`, 9:20); precios publicados y verificados (`2QipaMNIMLRGSQq1`, cada 15 min). Pregunta de facturación: la emite el comercio y el sistema deja lista la ficha de cada pedido pagado (`5aCkfwOwPRB9Neti`). Marcado BreadcrumbList y Service, sin FAQPage. Enlazada desde «Mayoristas B2B» en el menú «E-commerce» y el pie de todas las páginas, la baldosa y la sección `#mayoristas` de la portada y la 404. Si cambian esos workflows, actualizar la página. |
| `google-merchant-center/` | Página de canal (fase 2 del plan SEO, 23-sep-2026) para «Google Merchant Center» y «aparecer en Google Shopping» en Colombia. Describe el feed real de n8n (`5pHwflkFjEHMc4sW`, `GET /webhook/feed-hitair.xml` y `feed-ekivibes.xml`): se arma en cada descarga desde Medusa, una fila por variante con SKU, título con talla y color, `item_group_id`, precio al público en COP, foto principal y hasta 10 más, disponibilidad del inventario o `preorder` con `availability_date`, envío a Colombia y sin productos de prueba; Merchant Center lo descarga cada día y Meta cada hora. También: subcuentas de Hit-Air Colombia y Ekivibes bajo la cuenta avanzada (aprobada el 3-sep-2026), datos estructurados Product en las tiendas (precio, disponibilidad, envío y devoluciones), Perfil de Negocio verificado de las dos marcas e informe semanal de Search Console (`YDhQ3QOML5Ot1oba`, lunes 8:30). Lo de Google (fichas gratuitas sin costo; Búsqueda, Imágenes y pestaña de Shopping disponibles en Colombia; atributos obligatorios; sin garantía de que se muestren; elegibilidad del Perfil de Negocio) sale de la ayuda de Google consultada el 23-sep-2026: revisarla si Google cambia las reglas. Marcado BreadcrumbList y Service, sin FAQPage. Enlazada desde «Google» en el menú «E-commerce» y el pie de todas las páginas, la baldosa y la sección `#google` de la portada y la 404. |
| `automatizacion-empresas-de-servicios/` | Línea de servicios (23-sep-2026), separada de e-commerce en la barra, para empresas que venden servicios y no productos: desarrollo de software y agencias, salud (médicos y consultorios) y abogados y firmas. Presenta las piezas de n8n que ya corren en producción para Soulbyte y sus marcas — WhatsApp con menú, palabras clave y paso a una persona (el robot no interviene si el equipo respondió en las últimas 24 horas), WhatsApp Flows (una calificación baja abre una incidencia), registro de contactos del sitio y de anuncios de clic a WhatsApp con su origen, seguimiento de cotizaciones a los 3, 7 y 14 días, recordatorio de pago y cartera con plazos y cupo, series de correo y reactivación a los seis meses, y redes (publicaciones programadas, respuestas por reglas y resumen de los lunes) — y lo que se arma a la medida para cada oficio (`#desarrollo`, `#salud`, `#abogados`). Condiciones: Facebook e Instagram de otra empresa exigen el acceso avanzado de Meta; Meta cobra las plantillas directo a la empresa; en salud y derecho los mensajes automáticos no llevan datos clínicos ni detalles del caso. Sin inteligencia artificial: solo reglas. Ilustración del chat con la leyenda de datos de ejemplo. Marcado BreadcrumbList y Service, sin FAQPage. Enlazada desde el menú «Servicios» y el pie de todas las páginas, la franja `#servicios` y «Para quién» de la portada, y la 404. Si una de esas piezas cambia o deja de correr, actualizar la página. |
| `privacidad/` | Política de privacidad de la plataforma (22-sep-2026: datos de medición y públicos de Meta, páginas de Facebook y Messenger, y sus finalidades; 23-sep-2026: carrito del catálogo de WhatsApp convertido en carrito de la tienda y recordatorio único, y cuentas de TikTok que conecta el comercio: datos, finalidad, desconexión y borrado de tokens). |
| `assets/onboarding.js` | Lógica de las dos vías: lead + apertura del registro de Meta; SDK de Meta, evento `WA_EMBEDDED_SIGNUP`, `FB.login` con `response_type: 'code'`. Desde el 23-sep-2026 envía también `trafico_origen` (utm_source o dominio de procedencia; `google`, `bing` o `directo`), `trafico_entrada` y `trafico_referrer`: cada página guarda la primera página de la visita en `sessionStorage` (`sb_origen`, sin cookies). n8n los guarda en `tenants` solo la primera vez (primer contacto). |
| `robots.txt`, `sitemap.xml` | Indexación. `sitemap.xml` enviado a Search Console (propiedad de dominio `sc-domain:soulbyte.app`, verificada el 23-sep-2026). |
| `favicon.ico`, `icon.svg`, `apple-touch-icon.png` | Íconos del sitio (23-sep-2026): ICO de 16, 32 y 48 px (el que usa Google en los resultados), SVG con la «s» de Sora convertida en trazo y 180 px para iOS. |
| `assets/og-soulbyte.jpg`, `assets/logo-soulbyte-512.png` | Imagen para compartir (1200×630, `og:image` y `twitter:card` en todas las páginas) y logo del marcado Organization. |
| `404.html` | Página de error con la barra y el pie del sitio (`noindex`, enlaces absolutos a soulbyte.app para que también sirva en la copia de GitHub Pages). |
| `Caddyfile` | Servidor en Railway. Railpack lo usa en lugar de su plantilla (la procesa como plantilla de Go: `{{.StaticFileRoot}}`). Hace: `www.soulbyte.app` → `https://soulbyte.app{uri}` con 301; oculta `README.md`, `Caddyfile`, `Staticfile` y `.nojekyll`; caché de 30 días para imágenes y `no-cache` para CSS, JS y páginas (revalidan con ETag y responden 304 si no cambiaron: desde el 23-sep-2026, porque con 1 hora de caché una página nueva podía verse con la hoja de estilos anterior); errores con `/{código}.html`. Probar cualquier cambio con `caddy validate` y `caddy run` antes de publicar. |

## Mantenimiento

- Regla de Ivan (22-sep-2026): **toda funcionalidad nueva de n8n que entre en producción se agrega a este sitio en la misma sesión**: sección o tarjeta de la portada, «Qué incluye» si cambia la oferta, JSON-LD (`hasOfferCatalog`), este README, `sitemap.xml` (`lastmod`) y, si trata datos personales, `privacidad/`. Si también sirve a empresas de servicios, se agrega en `automatizacion-empresas-de-servicios/`.
- Solo se publica lo que corre en producción; lo que dependa de una aprobación de Meta o Google va en la nota de la sección.
- Regla de Ivan (23-sep-2026): el sitio no menciona Wompi ni Addi. Son cuentas de Hit-Air Colombia y Ekivibes como tiendas; Soulbyte todavía no es agregador ni integrador de ellos. Los pagos se describen de forma genérica (la pasarela de pagos del comercio).
- Publicación: el sandbox de la nube no puede hacer push a este repo. Se sube desde el Mac de Ivan: `~/Soulbyte/soulbyte-site` es un clon de este repo; un solo commit a `main` y push. Después del push, desplegar soulbyte.app con la API de Railway (servicio del proyecto `soulbyte`) y verificar en vivo (GitHub Pages actualiza la dirección anterior solo, en un par de minutos).
- SEO (plan en el proyecto, `claude/seo-soulbyte.md`): portada con marcado WebSite (nombre del sitio «Soulbyte», alternativos «Soulbyte Colombia» y «soulbyte.app»), Organization con logo, razón social y contacto, y Service; subpáginas con BreadcrumbList y, las páginas de canal, también Service. No se usa FAQPage: Google retiró ese resultado enriquecido en mayo de 2026.
- Estilos: `assets/soulbyte.css` (Sora para títulos, Inter para texto, esmeralda de marca). Las páginas enlazan `assets/soulbyte.css?v=2`: el `?v=` solo forzó la descarga de la versión nueva el 23-sep-2026; con `no-cache` ya no hace falta cambiarlo. Antes de publicar, revisar a 1280 y 390 px: sin desborde horizontal, contraste AA y los menús «E-commerce» y «Servicios» con ratón, teclado (Tab y Escape) y en móvil; solo uno queda abierto a la vez. Desde el 23-sep-2026 (barra con «E-commerce» y «Servicios») el menú se compacta en el botón de menú hasta 1120 px: a 1121 px la barra completa deja unos 107 px libres; si se agrega otro enlace, volver a medir. En móvil, el menú abierto se desplaza por dentro (hasta el 75 % del alto de la pantalla) para que los botones del final siempre se alcancen.

## Backend (n8n, `https://n8n.hitaircolombia.com`)

Workflow **"Soulbyte — onboarding WhatsApp de comercios (Embedded Signup, Tech Provider)"** (`va7vfi9YbATCZFlf`):

- `GET /webhook/soulbyte-onboarding-config` → `{app_id, config_id, version, habilitado, hosted_url, habilitado_hosted}` leído de NocoDB `parametros` (`meta_app_id`, `meta_es_config_id`, `meta_graph_version`, `meta_hosted_onboarding_url`). CORS: `https://soulbyte.app`, `https://www.soulbyte.app` y `https://satoshigod.github.io` (la respuesta devuelve el origen de la petición si está en la lista).
  - `habilitado` = hay `config_id` → se muestra el botón del registro integrado (SDK).
  - `habilitado_hosted` = hay `hosted_url` → se activa el botón "Empezar el registro en Meta".
- `POST /webhook/soulbyte-onboarding-lead` `{via, empresa, contacto_*, sitio_web}` → guarda/actualiza el comercio en NocoDB `tenants` (`m9flo5er05seh7b`) con `origen=hosted_signup`, `estado=lead`, y avisa al equipo (correo). Es lo que usa la vía guiada por Meta antes de abrir `hosted_url`.
- `POST /webhook/soulbyte-onboarding` `{code, waba_id, phone_number_id, business_id, event, empresa, contacto_*, sitio_web}` (vía integrada) →
  1. valida y genera `clave` + PIN de 6 dígitos;
  2. `GET /oauth/access_token` (client_secret desde la credencial n8n **"Meta — App Secret (Soulbyte n8n, query client_secret)"**) → business token;
  3. `POST /{waba_id}/subscribed_apps`;
  4. `GET /{phone_number_id}` y `GET /{waba_id}`;
  5. `POST /{phone_number_id}/register` solo si el número **no** está ya en Cloud API conectado;
  6. upsert en NocoDB `tenants` por `waba_id`;
  7. aviso interno por el portero del equipo y respuesta JSON a la página.

Tenant 1 (`clave=soulbyte`) es la operación propia: su token vive en el vault de n8n, no en la tabla.

**Conexión de TikTok (pendiente, 23-sep-2026).** NocoDB `parametros` ya tiene `tiktok_app_id` y `tiktok_auth_url` (vacíos) y `tiktok_conexion_activa = no`. Falta el workflow de n8n con `GET /webhook/soulbyte-tiktok-config` (mismo CORS que el onboarding de WhatsApp) y `POST /webhook/soulbyte-tiktok-conexion`, que cambia el código en `POST https://business-api.tiktok.com/open_api/v1.3/tt_user/oauth2/token/` (`client_id`, `client_secret`, `grant_type=authorization_code`, `auth_code`, `redirect_uri=https://soulbyte.app/conectar/tiktok/`), guarda la cuenta por `open_id` y avisa al equipo. El secreto de la app va en una credencial de n8n, nunca en la tabla ni en la página.

## Vía guiada por Meta (hosted Embedded Signup): cómo se completa un comercio

1. El comercio termina el registro en la página de Meta → Meta comparte su WABA con la app y envía el webhook `account_update` con `event=PARTNER_ADDED` (`waba_info.waba_id`, `owner_business_id`).
2. Con el token de usuario del sistema + `appsecret_proof`, `GET /{owner_business_id}/system_user_access_tokens?fetch_only=true` → business token del comercio.
3. `GET /{waba_id}/phone_numbers` → `phone_number_id`; luego `subscribed_apps` y `register` como en la vía integrada; upsert en `tenants` por `waba_id` (cruzar con el lead por correo/empresa).
4. El comercio debe añadir método de pago a su WABA (Meta le factura a él).

## Configuración en el panel de Meta (app "Soulbyte n8n" 1781576016329423)

- **Casos de uso → WhatsApp → Personalizar → Conviértete en proveedor de tecnología**: "2 de 2 pasos completados". La URL de **Proceso sin integración** es `hosted_url` → guardarla en NocoDB `parametros.meta_hosted_onboarding_url`.
- Para la vía integrada (opcional): **Inicio de sesión con Facebook para empresas → Configuración**: dominio permitido para el SDK `satoshigod.github.io`, URI de redirección `https://satoshigod.github.io/soulbyte/conectar/`; **Configuraciones → Crear a partir de plantilla → WhatsApp Embedded Signup** → copiar el ID a `parametros.meta_es_config_id`; **Configuración de la app → Básica → Dominios de la app**: `satoshigod.github.io`.
- **WhatsApp → Configuración → Webhooks**: suscribir `account_update` (además de `messages`).

Dominio definitivo **soulbyte.app** (23-sep-2026): URLs canónicas, `og:url`, JSON-LD, `sitemap.xml` y `robots.txt` apuntan a https://soulbyte.app/; los nodos Webhook de n8n aceptan los orígenes `https://soulbyte.app`, `https://www.soulbyte.app` y `https://satoshigod.github.io`, y las respuestas devuelven en `Access-Control-Allow-Origin` el origen de la petición si está en esa lista. Pendiente en Meta: agregar `soulbyte.app` a los dominios de la app y al dominio permitido del SDK, y la URI de redirección `https://soulbyte.app/conectar/` (la vía guiada por Meta no depende del dominio).
